<% provide(:title, @user.name) %>

<div class="container">
<h1><strong>メニュー一覧</strong></h1>
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <%= link_to "在庫一覧", user_stocks_path(@user), class: "btn btn-lg btn-primary btn-block" %><br>
            <%= link_to "在庫検索" , stock_search_user_stock_path(user_id: current_user.id, id: 1), class: "btn btn-lg btn-success btn-block" %><br>
            <%= link_to "倉庫配置表" , warehouse_arrangement_user_stock_path(@user), class: "btn btn-lg btn-warning btn-block" %><br>
        </div>
    </div>
</div>