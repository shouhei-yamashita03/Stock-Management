<% provide(:title, "warehouse_arrangement") %>
<div class="container">
    <h1><strong>倉庫配置表</strong></h1>
        <table class="table table-bordered table-condensed">
            <thead>
                <tr>
                    <th>写真</th>
                </tr>
            </thead>

            <tbody>
            <%# @stocks.each do |stock| %>
                <tr>
                    <td></td>
                </tr>
            <%# end %>
            </tbody>
        </table>
</div>